# JustGo — ملاحظات البنية الأولية

## ما الذي نريد بناءه؟

JustGo هو مختبر متصفح بعيد يهدف إلى إعطاء المستخدم جلسة متصفح منفصلة عن جهازه المحلي. لا يكفي أن يكون مجرد أداة لقطات أو scraper؛ التجربة المقصودة هي جلسة تفاعلية قصيرة، قابلة للانتهاء والتنظيف تلقائياً.

## حقائق معمارية مستخلصة

* المتصفح البعيد الحقيقي يعمل خارج جهاز المستخدم؛ الخادم ينفذ الرسم وJavaScript والطلبات الشبكية، بينما يتلقى المستخدم تدفقاً مرئياً أو نتائج API.
* نموذج العزل المفيد يبدأ ببيئة جديدة لكل جلسة ويمحو ملفات تعريف الارتباط والتخزين والعمليات عند انتهاء الجلسة.
* بث سطح مكتب كامل عبر WebSocket/VNC أو WebRTC يحتاج متصفحاً ذا واجهة، ونظام عرض، وخدمة جلسات مستمرة. ليس مناسباً لاستضافة صفحات ثابتة فقط.
* تشغيل متصفحات Headless عبر API مناسب للقطات، PDF، HTML المُرندر واختبارات قواعدية، لكنه لا يساوي جلسة تفاعلية تُبث للمستخدم.
* خدمة مجانية عامة بلا سقوف ليست واقعية: كل جلسة تستهلك ذاكرة ومعالجاً ونطاقاً شبكياً، وتحتاج حدود مدة وتزامن وتنظيف ومراقبة لإساءة الاستخدام.

## خياران عمليان للإصدار الأول

| الخيار | التجربة | البنية اللازمة | الحد الأولي المقترح |
|---|---|---|---|
| **مختبر المتصفح المحلي** | تشغيل واجهة تشغيل حقيقية على جهاز المستخدم عبر متصفحه؛ يفيد الاختبارات المحلية ولا ينقل التصفح إلى خادم. | موقع ثابت وواجهة فحص/إطلاق محلية. | مجاني فعلياً، لكن ليس عزل خادمي. |
| **جلسة متصفح بعيدة قصيرة** | متصفح Chromium في حاوية منفصلة يُعرض للمستخدم عبر بث تفاعلي. | خادم مستمر، Docker، Chromium، طبقة بث، مدير جلسات، عزل شبكة وحدود موارد. | جلسة واحدة قصيرة، لا تسجيل دخول أو رفع ملفات أو تنزيلات في البداية. |

## حدود أولية لازمة

1. لا وعود بأن Safari/iOS أو Windows حقيقية متاحة في النسخة الأولى؛ Chromium على Linux هو نقطة البداية الواقعية.
2. لا نعرض “عزل ضد البرمجيات الخبيثة” كضمان إلا بعد إعداد عزل طبقة النظام والشبكة واختباره.
3. لا تسجيل دخول أو حفظ جلسات أو رفع ملفات أو تنزيلات في أول نسخة عامة.
4. تُفرض مدة جلسة وتزامن محدودان، وتُمسح البيئة بعد الانتهاء.
5. يظل الاختبار على مواقع يملكها المستخدم أو روابط عامة مشروعة، مع منع الاستخدام الضار أو تجاوز الحماية.

## تحقيق تجربة «اضغط ثم استخدم»

لا ينبغي أن يرى الزائر طبقة الإقلاع أو إعداد صورة نظام. الحل العملي هو **تجهيز جلسة مسبقاً**: يحتفظ مدير الجلسات بعدد صغير من حاويات المتصفح الجاهزة، ثم يخصص واحدة للمستخدم عند الطلب ويصلها بواجهة البث. عند انتهاء المهلة تُدمر الجلسة ويُنشأ بديل نظيف في الخلفية.

| طبقة الخدمة | مهمتها |
|---|---|
| Launcher | يستقبل الرابط وخيار المتصفح ويطلب جلسة واحدة فقط. |
| Session manager | يخصص حاوية جاهزة، يفرض مدة وتزامناً، وينظف الجلسات المنتهية. |
| Interactive stream | ينقل شاشة المتصفح ومدخلات المستخدم عبر WebRTC أو عميل سطح مكتب ويب. |
| Runtime image | قالب مُسبق التجهيز يحتوي المتصفح وإعدادات الأمان؛ لا يراه المستخدم ولا يدخل إليه كصورة ISO. |

Kasm يوثق تجهيز الحاويات مسبقاً لتقليل زمن الانتظار، بينما Neko يقدم نموذج متصفح/سطح مكتب تفاعلي مبني على Docker وWebRTC. هذه أمثلة بحثية على طبقات يمكن تقييمها؛ لا يعني ذلك اعتماد أي منها قبل فحص العزل والترخيص.

تفاصيل مهمة من المصادر:

* مرحلة الجلسات في Kasm تحافظ على عدد مستهدف من الحاويات الجاهزة، وتعيد تجهيز بديل عندما تُخصص واحدة للمستخدم. تؤكد الوثائق أن توافق الصلاحيات وإعدادات الصوت/الحافظة/الرفع والتنزيل ضروري كي لا يتحول الطلب إلى إنشاء بطيء عند الطلب.
* توثق Kasm Workspaces نموذج حاويات بحدود CPU وذاكرة ومدة جلسة، وتوضح أن الحاوية يمكن أن تشغّل المتصفح مع عنوان إطلاق عند التخصيص. كما توثق الربط بخوادم RDP/VNC، ما يجعله نموذجاً مفيداً لمسار Windows جاهز لاحقاً.
* Neko مرخّص Apache-2.0 ويصف نفسه كمتصفح افتراضي مستضاف ذاتياً في Docker مع بث WebRTC؛ يناسب البث التفاعلي السلس، لكنه لا يقدم بمفرده مدير حوكمة وجلسات عامة آمنة.
* noVNC عميل VNC مفتوح المصدر يعمل من المتصفحات الحديثة عبر WebSocket وCanvas، لكنه يحتاج خادم VNC وWebSocket proxy؛ يلائم مسار RDP/VNC أو مضيف Mac/Windows أكثر من البث عالي الأداء.

## أصول التشغيل الكبيرة ومسار Windows الاحتياطي

* لا تُرفع صور النظام إلى Git العادي أو إلى GitHub Pages. Git LFS يحتفظ بمؤشر في المستودع ويحفظ الكائن الكبير خارج تاريخ Git، لكن GitHub يوضح أن Git LFS لا يعمل مع مواقع GitHub Pages. لذلك فإن المكان الصحيح لصورة النظام هو تخزين تشغيل خاص بالمضيف، أو سجل صور للحاويات، أو تخزين كائنات منفصل.
* تتيح Microsoft أداة إنشاء وسائط Windows 10 22H2 وصورة ISO من المصدر الرسمي، لكنها تنص على الحاجة إلى ترخيص لتثبيت Windows 10، كما أن دعم Windows 10 المجاني انتهى في 14 أكتوبر 2025. لا تُستخدم ISO كتنزيل للمستخدم؛ تُثبت مرة ضمن قالب جاهز على مضيف Windows مرخّص ثم تُقدَّم المتصفحات منه كجلسات جاهزة.

## قرار JustGo Engine

سنطوّر وحدات حقيقية لا مشروعاً متضخماً شكلياً. المحرك المستهدف يتكون من: عقدة إنشاء الجلسة، طابور التخصيص، سياسة حدود الموارد، بوابة رموز الجلسة قصيرة العمر، قناة الإدخال، قناة الفيديو/السطح، سجل صحي تشغيلي، وموصلات مضيف منفصلة لـ Chromium وWindows وmacOS.

واجهة الويب هي **طبقة تحكم وعرض** فقط. التنفيذ الفعلي للمتصفح أو نظام التشغيل يبقى في مضيف مشروع خارجها. هذا يمنع وعداً خاطئاً بأن HTML أو JavaScript وحدهما يقلعان Windows 10 أو Safari، لكنه يتيح تطوير جميع عقود الخدمة واختبارها الآن ثم ربط العتاد عند توفره.

لا يوجد سقف اصطناعي لحجم الأصول؛ صور النظام وطبقات التشغيل يمكن أن تكون كبيرة بالقدر اللازم، لكن لا تُحفظ في Git العادي ولا تتحول إلى تنزيل للمستخدم. لا يقاس نضج المشروع بعدد الأسطر، بل بوضوح العقود والاختبارات والأمان وقابلية التشغيل.

### عقد البث والإدخال المقترح

* تستخدم طبقة العرض اتصال WebRTC واحداً لكل جلسة: مسار فيديو/صوت اختياري للسطح البعيد، وقناة بيانات منفصلة للمدخلات والحالة.
* رسائل المدخلات صغيرة ومنظمة (`pointer`, `key`, `clipboard-policy`, `viewport`, `heartbeat`) ولا تحمل ملفات أو بيانات اعتماد. تحفظ الخوادم شرطاً زمنياً وتسلسل إدخال لمنع التدفق أو التلاعب.
* تضع بوابة الإشارة رمز جلسة قصير العمر وتتحقق منه قبل إصدار عرض/إجابة WebRTC. لا يُكشف عنوان المضيف الداخلي إلى العميل.
* لا يكفي اتصال Peer-to-peer في البيئات العامة؛ توثق WebRTC الحاجة المتوقعة إلى TURN عند تعذر الاتصال المباشر. لذلك TURN جزء من بنية تشغيل حقيقية وليس خياراً تجميلياً.
* تشفّر قنوات بيانات WebRTC عبر DTLS، لكن أمان JustGo لا يعتمد على التشفير وحده: العزل، الصلاحيات المحدودة، إبطال الرمز، والإنهاء الإجباري للجلسة مطلوبة أيضاً.

## المصادر

* Browserling — [Remote Browser](https://www.browserling.com/remote-browser)
* Browserling — [Browser Sandbox](https://www.browserling.com/browser-sandbox)
* Browserless — [Virtual Browsers Explained](https://www.browserless.io/blog/virtual-browser)
* Kasm — [Session Staging](https://kasmweb.com/docs/latest/guide/staging.html)
* Neko — [مستودع المشروع](https://github.com/m1k1o/neko)
* Kasm — [Workspaces](https://kasmweb.com/docs/latest/guide/workspaces.html)
* noVNC — [الموقع الرسمي](https://novnc.com/info.html)
* GitHub Docs — [Git Large File Storage](https://docs.github.com/repositories/working-with-files/managing-large-files/about-git-large-file-storage)
* Microsoft — [Download Windows 10](https://www.microsoft.com/software-download/windows10)
* WebRTC — [TURN server](https://webrtc.org/getting-started/turn-server)
* MDN — [Using WebRTC data channels](https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API/Using_data_channels)
