# JustGo — مراجعة تكامل محلية

> **حالة هذه الوثيقة:** مراجعة هندسية داخلية للمسار المحلي فقط. لا تعني جاهزية نشر عام، ولا تعني توافقاً مع Windows أو Linux أو BIOS أو جهاز حقيقي كامل.

## دليل التحقق الحالي

تُنفذ بوابة التحقق المعتمدة عبر `pnpm check`، وهي تجمع فحص TypeScript الصارم، واختبارات Vitest، وبناء Vite الإنتاجي. في آخر مراجعة تكامل مرّت **32 حزمة اختبار و104 اختبارات** مع البناء. المستودع `Maaazn/justgo.github.io` ما زال **خاصاً**؛ لم يُفعّل النشر أو GitHub Pages ضمن هذه المراجعة.

| العقد المتكامل | الدليل التنفيذي | النتيجة الضيقة |
|---|---|---|
| Core-64 وذاكرة الضيف | PML4 بصفحات 4KiB، عناوين canonical، ومفسر `REX`/`MOV`/`ADD`/`SUB`/`CMP`/`HLT` | برامج ضيف مصغرة فقط، لا مجموعة x86-64 عامة |
| تسليم المقاطعات | IDTR/IDT وIRQ/exception frame وIRETQ وIST محدود | الحلقات ring-0 فقط؛ لا انتقال امتياز كامل |
| توقيت المنصة | ترتيب ثابت: إدخال ثم CPU ثم PIT ثم RTC ثم storage ثم PIC ثم video | rendering خارج حالة الضيف ولا يغيرها |
| PIT وRTC | IRQ0 وIRQ8 يمران عبر PIC إلى IDT؛ تختبر EOI في fixture | نموذج جهاز محدود، لا RTC أو PIT كاملين |
| ATA ووسيط محلي | LBA28 PIO وDRQ/FIFO، وATA مجدول يبقى BSY حتى `Blob/File` prefetch | لا مسار إقلاع قرص أو DMA أو AHCI |
| ATA إلى IDT | اكتمال prefetch يولّد IRQ14 إلى PIC ثم بوابة Core-64 IDT | اختبارات sector جاهز وفشل prefetch وLBA محدد |
| replay | trace + PS/2 payload + snapshot سجلات وذاكرة مراقبة | يقارن العنوان الأول المختلف، ولا يصوّر RAM كاملاً |

## ما يثبته corpus الموحد

ينفذ corpus مصغر NOP ثم IRETQ ضمن PML4. يسجل scheduler دفعة لوحة مفاتيح PS/2، ويولد PIT IRQ0، ثم RTC IRQ8، ويمرر المتجهات عبر PIC إلى IDT. يعيد الاختبار استخراج دفعة PS/2 من trace، ويحقنها ثانية في scheduler، ثم يقارن trace والسجلات وbytes الذاكرة المراقبة. كما يثبت مسار divergence بأن تعطيل زمن RTC يغيّر trace وحالة `RIP` بدلاً من إخفاء الفرق.

## حدود مانعة لأي ادعاء بالإقلاع

| المجال | الحد الحالي |
|---|---|
| انتقال الامتياز | لا توجد حلقات user/kernel كاملة ولا `RSP0`/`SS` hardware frames أو TSS switching كامل |
| CPU | decoder وتعليمات ونماذج fault محدودة؛ لا توافق مع نواة أو محمل إقلاع عام |
| الذاكرة | لا large pages ولا NX ولا نموذج paging معماري كامل |
| التخزين | لا DMA أو AHCI أو partition/FS/boot chain؛ ATA لا يحمّل Blob كاملاً ولا يرفعه |
| firmware والأجهزة | لا BIOS/UEFI كامل، ولا USB guest controller، ولا نموذج VGA registers كامل |
| التوافق | لا دليل على إقلاع Windows أو Linux؛ لا تُضمَّن وسائط أنظمة أو مفاتيح أو صور مرخّصة |

## بوابة المرحلة التالية

لا يُنظر في تغيير الرؤية أو فتح الموقع قبل تنفيذ مراجعة نضج جديدة تتضمن: توسعة عقود CPU والحماية حيث تغطيها اختبارات، وربط boot contracts بوسائط قانونية يختارها المستخدم محلياً، وقياس تكراري لا يدّعي توافق نظام تشغيل، ثم تأكيد صريح ومحدّث من مالك المشروع. حتى ذلك الحين، يبقى المستودع خاصاً ويُعرض كمحرك تجريبي محلي محدود.
